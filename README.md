# some commands
```
python
import sys
sys.executable
exit()
"C:\...\python.exe" -m venv ".../venv"
source venv/Scripts(bin)/activate
pip install --trusted-host pypi.org --trusted-host pypi.python.org --trusted-host files.pythonhosted.org -r req.txt
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/TommyCalmman/copyBaseSberScript.git
git push -u origin main
```